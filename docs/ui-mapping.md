# 雙平台對照表

分兩部分：**元件對照**（UI Kit 每支元件在兩端有沒有對應實作）與 **主題色對照**（換主題時哪些 token 會變）。

# 元件對照表

UI Kit 的每一支元件，在 iOS 與 Android 上目前有沒有可以直接對應的共用元件。
**這份是給設計師用的**——畫面設計時先查這張表，就知道某個元件在哪一端要另外交付規格、
哪一端可以直接沿用既有樣式，不必等工程端回報才發現做不出來。

> 核對日期 2026-08-13 · 設計系統 `0.15.0` · iOS `develop`(7.43.1) · Android `develop`
> 核對方式：直接讀兩端原始碼的元件檔與 composable 宣告，**不看交接文件**（交接文件已知落後於實作）。

---
fu/
## 怎麼讀這張表

| 標記 | 意思 | 設計上代表什麼 |
|------|------|--------------|
| ✅ | 有共用元件，且對應到同一個東西 | 可以直接標註「用既有元件」，不必附規格 |
| ⚠️ | 有東西可以用，但不完全對位 | 樣式或行為會與設計稿有落差，**設計稿要額外說明差在哪** |
| ❌ | 沒有共用元件 | 各畫面自己刻，**每次設計都要附完整規格**，而且不同畫面會長得不一樣 |

「處理方式」欄寫的是**設計端接下來要做什麼**，不是工程端的修改步驟。
**排查過程、逐項數值與修法都不在這裡**——那些寫在雙平台對齊複核報告，欄位裡的 `C-xx` 就是報告裡的條目編號。

---

## 動作類

| 元件 | iOS | Android | 現況 | 處理方式 |
|------|:---:|:---:|------|---------|
| Button | ✅ | ✅ | 兩端樣式集齊全 | 直接標樣式名 |
| IconButton | ✅ | ✅ | **三邊對齊得最好的一支**，尺寸與樣式連順序都一致 | 無需處理 |
| Fab | ✅ | ✅ | 尺寸、圓角、圖示大小三邊吻合 | 無需處理 |
| ChipBar | ⚠️ | ❌ | iOS 只有單顆 Chip、沒有整列容器；Android 兩者皆無，各畫面自刻 | **Android 只補單顆 Chip**（選取、badge 對齊 iOS），整列橫向捲動由頁面自排（C-12） |

## 表單類

| 元件 | iOS | Android | 現況 | 處理方式 |
|------|:---:|:---:|------|---------|
| TextField | ⚠️ | ✅ | Android 一支到底；**iOS 新舊兩代並存**，8 個舊畫面仍是底線式（登入、OTP、載具綁定、會員卡編輯 ×2、推薦碼、刪除帳號說明、第三方登入選帳號） | 新畫面正常畫。**改到那 8 個舊畫面時外觀會從底線變框線，需重新出圖**（C-15） |
| TextArea | ❌ | ❌ | **兩端都沒有多行輸入元件**，唯一的多行欄位是「刪除會員帳號」的原因說明 | 新的多行需求**先提元件**，不要再各刻一份；在那之前標最小高度、行數、字數上限。**字數上限以 help text 顯示**（右對齊、不帶 icon），不做計數器元件（C-16） |
| SearchField | ⚠️ | ✅ | 兩端共用同一份設定、版面三邊一致；iOS 那支的文字色與字級仍取自舊體系 | 直接標「搜尋欄」即可。iOS 的色與字級**已裁決要修** |
| Select | ✅ | ⚠️ | 兩端都是「欄位 + 點開後的選單」兩層結構；Android 的欄位圓角、錯誤框線與 focus 態與規格有差 | 照「Select 欄位 + Sheet 選單」正常畫，**不必在 Android 的三支 selector 之間挑**。落差已列入待修（C-17） |
| Checkbox | ✅ | ✅ | 兩端都有，且都支援錯誤狀態與帶連結的同意條款 | 無需處理 |
| Radio | ✅ | ✅ | 兩端都有單顆樣式 | 無需處理 |
| RadioGroup | ⚠️ | ❌ | iOS 的群組行為併在單顆元件裡；Android 只有單顆，群組排列各畫面自組 | 選項間距、分隔線、單選互斥行為**需在設計稿標註**，Android 尤其不會自動套用 |
| Switch | ⚠️ | ✅ | Android 有設計系統版本；iOS 用系統開關，只有全域染成品牌色 | iOS 開關的尺寸與軌道樣式**無法依設計稿調整**，請沿用系統外觀 |
| PinInput | ❌ | ❌ | 沒有共用元件，但**手動對獎那組分格輸入兩端已各自做好且長得一樣**。注意那是「**顯示格 + 自訂數字鍵盤**」，不是逐格輸入框——設計系統 PinInput 的自動跳格、貼上分配在 App 上用不到。OTP 驗證兩端仍是一般單一輸入欄，只有 web 用 PinInput | 手動對獎照現況標即可，**不需附規格、也不需提元件需求**（已定案）。**雙平台目前沒有 OTP 驗證的需求**；日後要做時 **OTP 走逐格輸入框，與手動對獎那組分開**，屆時另提元件需求（C-13） |
| Slider | ❌ | ❌ | 兩端都沒有，**產品也沒有實際使用** | **已定案雙平台不開發共用元件**，日後要用再提 |
| FieldGroup | ✅ | ⚠️ | 這支是「單一欄位的容器」（label + 欄位 + 說明），**不是多欄位分組**。iOS 有共用元件；Android 各畫面自己寫，目前數值也對 | 直接標「欄位標題」即可，**不需要標分組間距那類數值** |
| FieldGroupHelpText | ✅ | ✅ | **兩端都有欄位層級的說明文字，與設計系統逐項吻合**，只是烘在輸入欄元件裡 | 無需處理。說明文字直接標在欄位下方 |

## 資訊呈現類

| 元件 | iOS | Android | 現況 | 處理方式 |
|------|:---:|:---:|------|---------|
| Tag | ⚠️ | ⚠️ | 兩端都有，但各缺一角：iOS 的「警告」誤用中獎的黃色；Android 少了「中獎淺色」與「警告深色」 | 兩項都已列入待修。標註警告標籤時請一併備註「應為橘色系」（C-04、C-07） |
| CategoryTag | ⚠️ | ✅ | Android 有膠囊標籤與圓形徽章兩支；iOS 的圓形徽章**畫面上有但沒有元件**，只有一種尺寸 | **已裁決雙邊對齊、iOS 要補。**圓形分類徽章尺寸依 Figma「Category badge」：Small 28 / Medium 40（C-18） |
| Badge | ⚠️ | ⚠️ | 紅點用在四處（TabBar、集點兌禮金幣數、金幣紀錄列表、IconButton）。**有元件的地方都對，沒用元件的地方各走各的**——兩端的 TabBar 紅點都不是規格的紅。帶數字的版本兩端都沒在用 | 紅點直接標即可，落差已列入待修（C-19）。**帶數字的通知數量標記：設計系統已備好（含 99+），但兩端目前不做**——產品真的要用時再提 |
| Avatar | ❌ | ❌ | 兩端都沒有，**產品也沒有實際使用** | **已定案雙平台不開發共用元件**，日後要用再提 |
| Divider | ❌ | ✅ | **顏色三邊已一致，而且水平／垂直分得很乾淨**（水平實色、垂直半透明）。Android 有元件，iOS 各畫面自寫。**不一致的是水平線要不要內縮** | 標「**列間**（內縮 16）」「**區塊間**（滿版）」或「**文字間**（垂直）」即可，**不要標色票**。內縮的收斂已列入待修（C-20） |
| ProgressBar | ⚠️ | ⚠️ | **通用長條進度產品沒在用**，設計系統先建。兩端唯一的進度特化版是**首頁預算區塊的半圓形進度** | **已定案雙平台不開發共用元件**，半圓進度維持現狀，不需另附規格 |
| ProgressGroup | ❌ | ❌ | 兩端都沒有，**產品也沒有實際使用** | **已定案雙平台不開發共用元件**，日後要用再提 |
| DottedController | ⚠️ | ⚠️ | **兩端都有、數值也幾乎都對**，但 iOS 是系統元件（尺寸間距改不動、沒有 overlap 版）、Android 是畫面私有 | **已裁決要建共用元件**，兩端都做。可以正常標圓點樣式，含照片上那種 overlap（C-21） |
| ListItem | ❌ | ⚠️ | iOS 完全沒有共用元件；Android 的設定類列已收斂成一支，但只有一種型態 | **已裁決要建共用元件，但排程已往後拉**（範圍最大、型態要先談），短期內不會有元件——列高、左右內距、次要文字字級**每張設計稿仍要標**（C-22） |
| CardItem | ❌ | ❌ | 兩端都沒有，卡片各畫面自刻 | **已裁決要建共用元件，但排程已往後拉**（同 ListItem），卡片內距與圓角**仍需逐張標註**（C-22） |
| ListHeader | ✅ | ✅ | **兩端都有，與設計系統逐項吻合**——用在個人設定頁的區塊表頭，只是寫在畫面裡不是獨立元件。Android 的表頭多一層 `background-sunken` 底色，是該平台常態用法，**不列為落差** | 直接標「區塊表頭」即可，不需附規格 |
| ListFooter | ✅ | ✅ | **兩端都有，逐項相同**——用在個人設定頁最下方的版本號 | 直接標「區塊表尾」即可，不需附規格 |

## 回饋類

| 元件 | iOS | Android | 現況 | 處理方式 |
|------|:---:|:---:|------|---------|
| Banner | ✅ | ✅ | 兩端都有，且都支援多種語意色 | 無需處理 |
| Dialog | ✅ | ✅ | 兩端都是自訂實作，都有插圖與危險操作樣式。Android 做不出上下直排按鈕，**但直排目前沒有實際應用** | 危險操作照現況用橫排，**設計稿不需標直排**（已裁決不修，C-08） |
| Toast | ✅ | ✅ | **兩種型態兩端都有**：等 API 的遮罩式（spinner + 階段文字 + 取消）與只有 spinner 的置中轉圈。落差只有 loading 的圓角 | 兩種都**直接標即可，不需附規格**。**操作結果一律標 SnackBar，不要標 Toast**（C-05、C-10） |
| SnackBar | ✅ | ⚠️ | 兩端都有。**Android 有兩套**，其中 View／XML 那套用的是舊色，**深色模式下兩套一深一淺** | 直接標「SnackBar」即可，深色模式的不一致已列入待修（C-23） |
| InAppNotification | ✅ | ❌ | iOS 有完整實作，Android 完全沒有 | 這是唯一一支 iOS 有、Android 全無的元件。**需先確認產品是否真的需要雙平台一致**（C-11） |
| Tooltip | ✅ | ✅ | 兩端都有，且都有浮動與行內兩種 | 方向數量未逐一核對，設計稿請標明指向 |
| Spinner | ⚠️ | ✅ | Android 有共用載入元件；iOS 各元件內各自使用系統轉圈，沒有統一入口 | iOS 載入指示器**尺寸與顏色由各元件自訂**，要指定時需個別標註 |
| PageStatus | ⚠️ | ✅ | **兩端都有，版面與文案也對得上**。落差在**插圖透明度的做法**——iOS 烘在圖檔裡，有兩張沒烘，三個畫面的插圖是實色 | 標「整頁狀態」＋狀態種類即可，**不需要標插圖透明度**。**「暫時沒有內容」已裁決不定預設文案**，每次自己給標題與說明；其餘四種沿用預設（C-27） |

## 頁面框架類

| 元件 | iOS | Android | 現況 | 處理方式 |
|------|:---:|:---:|------|---------|
| NavigationBar | ⚠️ | ✅ | Android 收在一支元件裡，34 個畫面共用。**iOS 沒有共用元件、五條路徑並存**，而且**沒有系統大標題**——8 個畫面的大標題各刻一份，內距與捲動收合行為都不同。**iOS 的底線目前不屬於導覽列**，是內容區自己補的，沒補就沒有 | 標「一般標題／大標題」＋「底線有／無」即可，**不要標高度與字重**（**大標題字重定案 700**）。**iOS 要指明走哪一條路徑**。**已列為該建元件**（C-26）。已定案：**底線屬於元件的一部分、iOS 要對齊**；**收合是逐頁客製，元件不內建**，要收合請在該頁另外標；**首頁沒有標題是定案**（logo + 三顆按鈕，不是規格的 `type="home"`） |
| TabBar | ⚠️ | ⚠️ | **兩端都是平台原生容器、整個 app 各只有一座**，樣式已收斂在單一處，順序、圖示與顏色三邊吻合。落差在標籤字級與紅點色 | **不必做成共用元件。**標「底部導覽」即可，不需附版面規格。**字級已裁決以 10 為準，只有 Android 要改**（C-25），iOS 與上緣分隔線都維持現狀；紅點落差已列入待修（C-19） |
| Tabs | ✅ | ✅ | 兩端都有分頁切換列 | **Tabs 是導覽**（分頁切換、頁面內章節錨點）；篩選與選參數屬於 ChipBar，不要拿 Tabs 頂替 |
| PageNavigation | ⚠️ | ⚠️ | 沒有共用元件，但這個樣式兩端各有 2 支實作、涵蓋 3 個畫面。**分類明細那組已三邊對齊**，發票列表那組兩端互不一致也不符規格 | 標「上一頁／標籤／下一頁」＋**標籤可不可點**＋**要不要下底線**即可（元件都做得出來）。**已裁決要建共用元件，以分類明細那組為基準**（C-24） |
| Sheet | ✅ | ✅ | 兩端都有底部彈出面板 | 無需處理 |
| SheetHeader | ⚠️ | ✅ | Android 有獨立的面板標題元件；iOS 內建在面板裡，**不能單獨使用** | iOS 的面板標題**只能用內建的兩種**（大標題／一般），超出這兩種需另提 |

---

## 反向清單：原生有、設計系統沒有

這些是兩端已經在用、但 UI Kit 裡沒有對應元件的東西。**設計時如果需要這些，是有東西可以用的**，只是設計系統沒有描述它們。

| 東西 | iOS | Android | 說明 |
|------|:---:|:---:|------|
| 數字鍵盤 | ✅ | ✅ | 自訂的九宮格數字鍵盤（含清除、退格）。兩端都只服務手動對獎 |
| 年月選擇 | ✅ | ✅ | 兩端都有滾輪式年月選擇 |
| 日期選擇 | — | ✅ | Android 另有自訂日期對話框，但**已定案雙平台不做共用元件**，採原生或套件 picker（見 [usage.md](./usage.md)）——設計稿不需標註樣式 |
| 條碼／QR 顯示 | ✅ | ✅ | 發票條碼與 QR 的顯示元件 |
| 下拉重新整理 | — | ✅ | 含品牌色指示器 |
| 載入骨架動畫 | — | ✅ | 內容載入時的閃爍佔位 |
| 動畫播放 | ✅ | — | Lottie 動畫容器 |
| 評分邀請 | ✅ | — | App Store 評分引導 |
| 分享列 | ✅ | — | 分享按鈕群組 |

---

# 主題色對應表

App 有「換主題色」功能：用戶選定主題後，**品牌色系的 token 會被整組換成該主題的顏色**，明暗模式各換一套。
本節的規格來源是 Figma [🎨 Design System 2025 — Color token / Theme](https://www.figma.com/design/FcUQOflk2OdZ2z2imVEcIw/%F0%9F%8E%A8-Design-System-2025?node-id=1128-6580)。

## 六套主題的三種叫法

同一套主題在三個地方叫不同名字，交接時最容易對錯。**設計稿用中文名，程式與 token 用英文名。**

| 產品名（Figma 表頭） | 設計系統 token | Figma 色階名 | 色相 |
|------|------|------|------|
| 白雪公主的蘋果 | `theme-apple` | `theme-red-*` | 紅 |
| 綠光 | `theme-green` | `theme-green-*` | 綠 |
| 少女粉色系 | `theme-girl` | `theme-pink-*` | 粉 |
| 湖中女神 | `theme-lake` | `theme-teal-*` | 藍綠 |
| 薰衣草 | `theme-lavender` | `theme-purple-*` | 紫 |
| 鐵漢柔情 | `theme-rock` | `theme-gray-*` | 灰藍 |

預設（未換主題）用的是 `blue-*` 色階。

> **這 90 個 `*-theme-*` token 不在設計系統的輸出裡**（2026-08-13 定案）。
> 三邊都 0 引用——換主題實際上是**伺服器下發 JSON 覆寫下表那 15 個 brand token**
> （iOS `ThemeManager` 讀 `theme.json`、Android `ThemePack`），不是去讀 `theme-apple-*` 這種名字。
> 色盤只留在 Figma；要拿 hex 值請回 Figma 或 `tokens/tokens.json` 母版查。

## 哪些 token 會跟著主題變

Figma 標示會跟主題走的共 **17 個**；**兩個平台實際實作的是 15 個**。
「階」欄位是該 token 在明／暗模式下取用的色階號——六套主題都取同一階，只是換色相。

| Token | 明亮模式 | 深色模式 | iOS | Android | 用途 |
|------|:---:|:---:|:---:|:---:|------|
| `color-content-brand-default` | 500 | 400 | ✅ | ✅ | 內容要呈現品牌色，例如主要按鈕 |
| `color-content-brand-hover` | 600 | 300 | ✅ | ✅ | 同上，hover 狀態 |
| `color-content-brand-active` | 700 | 200 | ✅ | ✅ | 同上，點擊狀態 |
| `color-content-brand-subtle` | 300 | 600 | ✅ | ✅ | 內容在對比高的品牌色背景上 |
| `color-content-brand-bold` | 700 | 200 | ✅ | ✅ | 內容在對比低的品牌色背景上 |
| `color-content-inverse-brand` | 400 | 500 | ✅ | ✅ | 內容在明亮模式的深色背景上 |
| `color-content-fixed-brand` | 500 | 500 | ✅ | ✅ | 內容在明暗模式皆為淺色的背景上 |
| `color-background-brand-default` | 500 | 預設 500 / 主題 400 | ✅ | ✅ | 背景要呈現品牌色，例如主要按鈕 |
| `color-background-brand-hover` | 600 | 300 | ✅ | ✅ | 同上，hover 狀態 |
| `color-background-brand-active` | 700 | 200 | ✅ | ✅ | 同上，點擊狀態 |
| `color-background-brand-subtle` | 300 | 600 | ✅ | ✅ | 淺色的品牌色背景 |
| `color-background-brand-subtlest` | 100 | 800 | ✅ | ✅ | 對比低的品牌色背景 |
| `color-background-brand-bold` | 700 | 200 | ✅ | ✅ | 深色的品牌色背景 |
| `color-background-brand-boldest` | 800 | 100 | ✅ | ✅ | 對比高的品牌色背景 |
| `color-border-brand` | 500 | 400 | ✅ | ✅ | 品牌色調的元件邊線 |
| `color-content-brand-gradient-primary` | 500 | 500（同明亮） | ⚠️ | ⚠️ | 品牌漸層的主要顏色 |
| `color-content-brand-gradient-secondary` | 預設 cyan-400 / 主題 300 | 同明亮 | ⚠️ | ⚠️ | 品牌漸層的次要顏色 |

> 漸層的兩個 token 是表上唯一**明暗模式取同一階**的——其餘 15 個都會隨模式換階。

## 漸層色

首頁歡迎詞的漸層跑動效果會跟著主題換色，但雙平台都沒有使用這兩個 gradient token。

| 情境 | iOS | Android |
|------|------|------|
| 預設主題 | 硬寫 `#00D3F7` + `#3560FF`（兩色） | 硬寫 `#3560FF` + `#00CDF1` + `#3560FF`（三色） |
| 已換主題 | `content-brand-subtle` + `content-brand-default` | `content-brand-default` + `content-brand-subtle` + `content-brand-default` |

雙平台漸層色在深色模式的變色時機點也不同

| 情境 | 行為 | 是否符合規格 |
|------|------|:---:|
| 預設主題 | 硬寫 hex，明暗共用 → 不變 | ✅ |
| 已換主題 · Android | 隨模式重新解析 → 會變色 | ❌ |
| 已換主題 · iOS | 切換當下不變，該區塊重建後才變 → 延遲變色 | ❌ |

> **已決定維持現狀**（2026-08-12）：漸層的實作方式可接受，不做更動。
> 上表列出的差異僅供日後查閱，不需開票處理。設計稿標註漸層時直接沿用現況即可。

## 舊 token 的對應

少數頁面（載具管理、我的發票、消費分析）仍在用舊版 token，雙平台有新舊對應機制。

| 舊 token | 對應到 |
|------|------|
| `themeLight` | `color-background-brand-subtle` |
| `themeMedium` | `color-background-brand-default` |
| `themeDark` | `color-background-brand-bold` |

---

## 這份表沒有回答的事

- **只查到「元件存不存在」與「主要變體對不對得上」**，沒有逐一核對每個屬性的數值。標 ✅ 的元件仍可能有間距或字級差異。
- **未逐項核對**：Tooltip 的方向數量、ListItem 的尾端元素。
- 樣式層級的落差（字重、字距、等寬字）不在這張表裡，見雙平台對齊複核報告。
- **每一列的證據、實測數值與修法也都在那份報告裡**，本表只保留設計端需要的結論。

## 維護方式

**這份表會過期。** 兩端的元件持續在長，而這份是 2026-08-13 的快照。
建議在每次設計系統改版、或每季規劃前重新核對一次；核對方式是直接讀兩端原始碼的元件清單，
